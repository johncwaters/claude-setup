# Repos the "repos" step clones and keeps synced.
# Format: one entry per line, <path relative to home (%USERPROFILE% or $HOME)>=<clone url>.
# Copy this file to setup/repos.txt and list your own repos. repos.txt is gitignored,
# so machine-specific (and private) clone urls never leave your machine; setup/collect.ps1
# and setup/collect.sh regenerate it from the glissa project list. Either slash works;
# apply.sh translates backslashes.
Projects\glissa=https://github.com/johncwaters/glissa
