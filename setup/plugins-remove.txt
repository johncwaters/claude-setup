# Retired Claude Code plugins. Apply tears down any of these still present so machines
# converge: the settings.json enabledPlugins and extraKnownMarketplaces keys, the plugin
# and marketplace registries under plugins/, the cached plugin dirs, plus the extra npm
# shims and state dirs named per entry.
# Format: <plugin>@<marketplace> [shims=a,b] [state=rel/dir,rel/dir]
#   shims: command names to delete from the npm global bin dir (bare, .cmd and .ps1)
#   state: dirs to delete, relative to the home directory
oh-my-claudecode@omc shims=omc,omc-cli state=.claude/.omc,.omc
